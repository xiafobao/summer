spring:
  cloud:
    config:
      name: globalConfig
      profile: test
      label: master
      discovery:
        enabled: true
        service-id: spring-cloud-config-server-demo


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


